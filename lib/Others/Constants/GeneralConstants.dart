import 'package:flutter/material.dart';
import 'dart:ui';

double STATUSBAR_H = MediaQueryData.fromWindow(window).padding.top;
double SCREEN_W = MediaQueryData.fromWindow(window).size.width;
double SCREEN_H = MediaQueryData.fromWindow(window).size.height;
